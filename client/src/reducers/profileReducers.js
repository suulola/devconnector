const initialState = {
  profile: null,
  profiles: null,
  loading: false,
};

const profileReducers = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default profileReducers;
